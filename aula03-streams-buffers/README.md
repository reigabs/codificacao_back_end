# Aula 03 — Streams e Buffers

## Objetivo

Praticar o uso de Streams no Node.js para trabalhar com arquivos, realizando escrita e leitura de dados de forma eficiente, além de observar o consumo de memória durante o processamento.

## Desenvolvimento

Durante a aula, foi criada a pasta `aula03-streams-buffers` para desenvolver atividades relacionadas ao uso de Streams e processamento de arquivos no Node.js.

### 1. Configuração do projeto

Foi criado o arquivo `package.json` e configurado o projeto para utilizar módulos ESM:

```json
{
  "type": "module"
}